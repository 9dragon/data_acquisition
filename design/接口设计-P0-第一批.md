# 接口设计文档 - P0第一批

**版本**: V1.0
**编写日期**: 2026-03-26
**适用模块**: 登录认证、角色权限、工序管理、项目阶段

---

## 目录

1. [接口设计规范](#接口设计规范)
2. [登录认证模块](#登录认证模块)
3. [角色权限模块](#角色权限模块)
4. [工序管理模块](#工序管理模块)
5. [项目阶段模块](#项目阶段模块)
6. [错误码说明](#错误码说明)

---

## 接口设计规范

### 基础规范

- **基础路径**: `/api/v1`
- **认证方式**: `Authorization: Bearer {token}`
- **请求格式**: `application/json`
- **响应格式**: `application/json`
- **字符编码**: `UTF-8`

### 统一响应格式

#### 成功响应

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {},
  "timestamp": "2026-03-26T10:00:00Z"
}
```

#### 分页响应

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "list": [],
    "total": 100,
    "page": 1,
    "pageSize": 10,
    "totalPages": 10
  },
  "timestamp": "2026-03-26T10:00:00Z"
}
```

#### 错误响应

```json
{
  "code": 1001,
  "message": "用户名或密码错误",
  "details": {},
  "timestamp": "2026-03-26T10:00:00Z"
}
```

### HTTP方法规范

| 方法 | 说明 | 幂等性 |
|------|------|--------|
| GET | 查询资源 | 是 |
| POST | 创建资源 | 否 |
| PUT | 全量更新资源 | 是 |
| PATCH | 部分更新资源 | 是 |
| DELETE | 删除资源 | 是 |

---

## 登录认证模块

### 1. 用户登录

**接口**: `POST /api/v1/auth/login`

**说明**: 用户登录，返回JWT Token

**请求参数**:

```json
{
  "username": "admin",
  "password": "admin123"
}
```

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| username | string | 是 | 用户名 |
| password | string | 是 | 密码 |

**成功响应** (200):

```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "refreshToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "tokenType": "Bearer",
    "expiresIn": 7200,
    "userInfo": {
      "id": "user_001",
      "username": "admin",
      "name": "系统管理员",
      "email": "admin@example.com",
      "avatar": "https://example.com/avatar.jpg",
      "roles": ["admin"],
      "permissions": ["project:create", "project:edit", "*"]
    }
  },
  "timestamp": "2026-03-26T10:00:00Z"
}
```

**错误响应**:

- 401: 用户名或密码错误
- 403: 用户已被禁用
- 500: 服务器内部错误

**Token说明**:
- Access Token: 2小时过期
- Refresh Token: 7天过期
- Token包含: userId, username, roles, expireTime

---

### 2. Token刷新

**接口**: `POST /api/v1/auth/refresh`

**说明**: 使用Refresh Token刷新Access Token

**请求参数**:

```json
{
  "refreshToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

**成功响应** (200):

```json
{
  "code": 200,
  "message": "刷新成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "expiresIn": 7200
  },
  "timestamp": "2026-03-26T10:00:00Z"
}
```

**错误响应**:
- 401: Refresh Token无效或已过期
- 500: 服务器内部错误

---

### 3. 用户注销

**接口**: `POST /api/v1/auth/logout`

**说明**: 用户注销，清除服务端Token缓存

**请求头**:
```
Authorization: Bearer {token}
```

**成功响应** (200):

```json
{
  "code": 200,
  "message": "注销成功",
  "data": null,
  "timestamp": "2026-03-26T10:00:00Z"
}
```

---

### 4. 获取当前用户信息

**接口**: `GET /api/v1/auth/userinfo`

**说明**: 获取当前登录用户的信息

**请求头**:
```
Authorization: Bearer {token}
```

**成功响应** (200):

```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "id": "user_001",
    "username": "admin",
    "name": "系统管理员",
    "email": "admin@example.com",
    "phone": "13800138000",
    "avatar": "https://example.com/avatar.jpg",
    "roles": [
      {
        "id": "role_001",
        "code": "admin",
        "name": "超级管理员"
      }
    ],
    "permissions": [
      "project:view",
      "project:create",
      "project:edit",
      "project:delete",
      "*"
    ],
    "lastLoginTime": "2026-03-26T09:55:00Z",
    "lastLoginIp": "192.168.1.100"
  },
  "timestamp": "2026-03-26T10:00:00Z"
}
```

**错误响应**:
- 401: 未登录或Token无效

---

## 角色权限模块

### 1. 获取权限列表

**接口**: `GET /api/v1/permissions`

**说明**: 获取所有权限列表（树形结构）

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| type | string | 否 | 权限类型：menu/button/api |
| status | int | 否 | 状态：1启用 0禁用 |

**成功响应** (200):

```json
{
  "code": 200,
  "message": "查询成功",
  "data": [
    {
      "id": "perm_001",
      "code": "project",
      "name": "项目管理",
      "type": "menu",
      "parentId": null,
      "path": "/project",
      "method": null,
      "description": "项目管理模块",
      "sortOrder": 1,
      "status": 1,
      "children": [
        {
          "id": "perm_002",
          "code": "project:view",
          "name": "查看项目",
          "type": "button",
          "parentId": "perm_001",
          "path": "/project/list",
          "method": "GET",
          "description": "查看项目列表",
          "sortOrder": 1,
          "status": 1,
          "children": []
        },
        {
          "id": "perm_003",
          "code": "project:create",
          "name": "创建项目",
          "type": "button",
          "parentId": "perm_001",
          "path": "/project/create",
          "method": "POST",
          "description": "创建新项目",
          "sortOrder": 2,
          "status": 1,
          "children": []
        }
      ]
    }
  ],
  "timestamp": "2026-03-26T10:00:00Z"
}
```

---

### 2. 获取角色列表

**接口**: `GET /api/v1/roles`

**说明**: 获取角色列表（分页）

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | int | 否 | 页码，默认1 |
| pageSize | int | 否 | 每页数量，默认10 |
| name | string | 否 | 角色名称（模糊搜索） |
| code | string | 否 | 角色编码（模糊搜索） |

**成功响应** (200):

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "list": [
      {
        "id": "role_001",
        "code": "admin",
        "name": "超级管理员",
        "description": "拥有所有权限",
        "isSystem": 1,
        "userCount": 3,
        "permissionCount": 50,
        "createdAt": "2026-03-01T10:00:00Z",
        "updatedAt": "2026-03-26T10:00:00Z"
      },
      {
        "id": "role_002",
        "code": "project_manager",
        "name": "项目经理",
        "description": "负责项目管理",
        "isSystem": 0,
        "userCount": 10,
        "permissionCount": 15,
        "createdAt": "2026-03-01T10:00:00Z",
        "updatedAt": "2026-03-26T10:00:00Z"
      }
    ],
    "total": 2,
    "page": 1,
    "pageSize": 10,
    "totalPages": 1
  },
  "timestamp": "2026-03-26T10:00:00Z"
}
```

---

### 3. 创建角色

**接口**: `POST /api/v1/roles`

**说明**: 创建新角色

**请求参数**:

```json
{
  "code": "engineer",
  "name": "实施工程师",
  "description": "负责设备实施工作",
  "permissionIds": ["perm_005", "perm_006", "perm_007"]
}
```

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| code | string | 是 | 角色编码，唯一 |
| name | string | 是 | 角色名称 |
| description | string | 否 | 角色描述 |
| permissionIds | array | 否 | 权限ID列表 |

**成功响应** (200):

```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "id": "role_003",
    "code": "engineer",
    "name": "实施工程师",
    "description": "负责设备实施工作",
    "isSystem": 0,
    "createdAt": "2026-03-26T10:00:00Z"
  },
  "timestamp": "2026-03-26T10:00:00Z"
}
```

**错误响应**:
- 2001: 角色编码已存在

---

### 4. 更新角色

**接口**: `PUT /api/v1/roles/:id`

**说明**: 更新角色信息

**请求参数**:

```json
{
  "name": "实施工程师（更新）",
  "description": "负责设备实施和配置工作",
  "permissionIds": ["perm_005", "perm_006", "perm_007", "perm_008"]
}
```

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| name | string | 否 | 角色名称 |
| description | string | 否 | 角色描述 |
| permissionIds | array | 否 | 权限ID列表 |

**成功响应** (200):

```json
{
  "code": 200,
  "message": "更新成功",
  "data": {
    "id": "role_003",
    "code": "engineer",
    "name": "实施工程师（更新）",
    "description": "负责设备实施和配置工作",
    "updatedAt": "2026-03-26T10:05:00Z"
  },
  "timestamp": "2026-03-26T10:05:00Z"
}
```

**错误响应**:
- 4004: 角色不存在
- 2003: 系统预置角色不能修改

---

### 5. 删除角色

**接口**: `DELETE /api/v1/roles/:id`

**说明**: 删除角色

**成功响应** (200):

```json
{
  "code": 200,
  "message": "删除成功",
  "data": null,
  "timestamp": "2026-03-26T10:00:00Z"
}
```

**错误响应**:
- 4004: 角色不存在
- 2002: 角色下有用户，不能删除
- 2003: 系统预置角色不能删除

---

### 6. 分配角色权限

**接口**: `POST /api/v1/roles/:id/permissions`

**说明**: 为角色分配权限

**请求参数**:

```json
{
  "permissionIds": ["perm_001", "perm_002", "perm_003"]
}
```

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| permissionIds | array | 是 | 权限ID列表 |

**成功响应** (200):

```json
{
  "code": 200,
  "message": "分配成功",
  "data": {
    "roleId": "role_003",
    "permissionCount": 3
  },
  "timestamp": "2026-03-26T10:00:00Z"
}
```

---

## 工序管理模块

### 1. 获取工序列表

**接口**: `GET /api/v1/processes`

**说明**: 获取工序列表

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| projectId | string | 是 | 项目ID |
| page | int | 否 | 页码，默认1 |
| pageSize | int | 否 | 每页数量，默认10 |

**成功响应** (200):

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "list": [
      {
        "id": "process_001",
        "projectId": "project_001",
        "projectName": "XX工厂数采项目",
        "name": "焊接工序",
        "code": "WELD_001",
        "description": "焊接车间工序",
        "sortOrder": 1,
        "createdAt": "2026-03-01T10:00:00Z",
        "updatedAt": "2026-03-26T10:00:00Z"
      },
      {
        "id": "process_002",
        "projectId": "project_001",
        "projectName": "XX工厂数采项目",
        "name": "组装工序",
        "code": "ASMB_001",
        "description": "组装车间工序",
        "sortOrder": 2,
        "createdAt": "2026-03-01T10:00:00Z",
        "updatedAt": "2026-03-26T10:00:00Z"
      }
    ],
    "total": 2,
    "page": 1,
    "pageSize": 10,
    "totalPages": 1
  },
  "timestamp": "2026-03-26T10:00:00Z"
}
```

---

### 2. 创建工序

**接口**: `POST /api/v1/processes`

**说明**: 创建新工序

**请求参数**:

```json
{
  "projectId": "project_001",
  "name": "涂装工序",
  "code": "COAT_001",
  "description": "涂装车间工序",
  "sortOrder": 3
}
```

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| projectId | string | 是 | 项目ID |
| name | string | 是 | 工序名称 |
| code | string | 否 | 工序编号 |
| description | string | 否 | 工序描述 |
| sortOrder | int | 否 | 排序序号 |

**成功响应** (200):

```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "id": "process_003",
    "projectId": "project_001",
    "name": "涂装工序",
    "code": "COAT_001",
    "description": "涂装车间工序",
    "sortOrder": 3,
    "createdAt": "2026-03-26T10:00:00Z"
  },
  "timestamp": "2026-03-26T10:00:00Z"
}
```

---

### 3. 更新工序

**接口**: `PUT /api/v1/processes/:id`

**说明**: 更新工序信息

**请求参数**:

```json
{
  "name": "涂装工序（更新）",
  "description": "涂装车间工序，包含喷漆和烘干",
  "sortOrder": 3
}
```

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| name | string | 否 | 工序名称 |
| code | string | 否 | 工序编号 |
| description | string | 否 | 工序描述 |
| sortOrder | int | 否 | 排序序号 |

**成功响应** (200):

```json
{
  "code": 200,
  "message": "更新成功",
  "data": {
    "id": "process_003",
    "name": "涂装工序（更新）",
    "description": "涂装车间工序，包含喷漆和烘干",
    "sortOrder": 3,
    "updatedAt": "2026-03-26T10:05:00Z"
  },
  "timestamp": "2026-03-26T10:05:00Z"
}
```

---

### 4. 删除工序

**接口**: `DELETE /api/v1/processes/:id`

**说明**: 删除工序

**成功响应** (200):

```json
{
  "code": 200,
  "message": "删除成功",
  "data": null,
  "timestamp": "2026-03-26T10:00:00Z"
}
```

**错误响应**:
- 4004: 工序不存在
- 2004: 工序下有设备类型，不能删除

---

### 5. 批量排序工序

**接口**: `PUT /api/v1/processes/reorder`

**说明**: 批量调整工序排序

**请求参数**:

```json
{
  "processes": [
    {"id": "process_001", "sortOrder": 2},
    {"id": "process_002", "sortOrder": 1},
    {"id": "process_003", "sortOrder": 3}
  ]
}
```

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| processes | array | 是 | 工序列表，包含id和sortOrder |

**成功响应** (200):

```json
{
  "code": 200,
  "message": "排序成功",
  "data": null,
  "timestamp": "2026-03-26T10:00:00Z"
}
```

---

## 项目阶段模块

### 1. 获取阶段列表

**接口**: `GET /api/v1/stages`

**说明**: 获取项目阶段列表

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| isSystem | int | 否 | 是否系统预置：1是 0否 |
| progressMode | string | 否 | 推进方式：by_task/by_device |
| status | int | 否 | 状态：1启用 0禁用 |

**成功响应** (200):

```json
{
  "code": 200,
  "message": "查询成功",
  "data": [
    {
      "id": "stage_001",
      "key": "pre_sale",
      "name": "售前调研",
      "description": "项目售前阶段的调研和评估",
      "icon": "Search",
      "color": "#52c41a",
      "progressMode": "by_device",
      "isSystem": 1,
      "defaultWeight": 0,
      "taskTemplates": null,
      "sortOrder": 1,
      "status": 1,
      "createdAt": "2026-03-01T10:00:00Z",
      "updatedAt": "2026-03-26T10:00:00Z"
    },
    {
      "id": "stage_002",
      "key": "preparation",
      "name": "准备阶段",
      "description": "施工前的准备工作",
      "icon": "Setup",
      "color": "#1890ff",
      "progressMode": "by_device",
      "isSystem": 1,
      "defaultWeight": 20,
      "taskTemplates": [
        {
          "id": "task_001",
          "key": "site_survey",
          "name": "现场勘察",
          "description": "到现场进行设备勘察和点位确认",
          "defaultWeight": 20,
          "materialRequirements": [
            {
              "key": "before_photo",
              "name": "安装前照片",
              "description": "设备安装前的现场照片",
              "fileType": "image",
              "required": true,
              "minCount": 3,
              "maxCount": 10,
              "acceptTypes": ["image/jpeg", "image/png"]
            }
          ]
        }
      ],
      "sortOrder": 2,
      "status": 1,
      "createdAt": "2026-03-01T10:00:00Z",
      "updatedAt": "2026-03-26T10:00:00Z"
    }
  ],
  "timestamp": "2026-03-26T10:00:00Z"
}
```

---

### 2. 创建阶段

**接口**: `POST /api/v1/stages`

**说明**: 创建新阶段（仅自定义阶段）

**请求参数**:

```json
{
  "key": "custom_stage",
  "name": "自定义阶段",
  "description": "项目特定阶段",
  "icon": "Setting",
  "color": "#fa8c16",
  "progressMode": "by_device",
  "defaultWeight": 10,
  "sortOrder": 7
}
```

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| key | string | 是 | 阶段唯一标识 |
| name | string | 是 | 阶段名称 |
| description | string | 否 | 阶段描述 |
| icon | string | 否 | 图标名称 |
| color | string | 否 | 显示颜色 |
| progressMode | string | 是 | 推进方式：by_task/by_device |
| defaultWeight | int | 否 | 默认权重，0-100 |
| sortOrder | int | 否 | 排序序号 |

**成功响应** (200):

```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "id": "stage_007",
    "key": "custom_stage",
    "name": "自定义阶段",
    "description": "项目特定阶段",
    "icon": "Setting",
    "color": "#fa8c16",
    "progressMode": "by_device",
    "isSystem": 0,
    "defaultWeight": 10,
    "sortOrder": 7,
    "createdAt": "2026-03-26T10:00:00Z"
  },
  "timestamp": "2026-03-26T10:00:00Z"
}
```

**错误响应**:
- 2001: 阶段标识已存在

---

### 3. 更新阶段

**接口**: `PUT /api/v1/stages/:id`

**说明**: 更新阶段信息

**请求参数**:

```json
{
  "name": "自定义阶段（更新）",
  "description": "项目特定阶段，包含特殊要求",
  "defaultWeight": 15
}
```

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| name | string | 否 | 阶段名称 |
| description | string | 否 | 阶段描述 |
| defaultWeight | int | 否 | 默认权重 |

**成功响应** (200):

```json
{
  "code": 200,
  "message": "更新成功",
  "data": {
    "id": "stage_007",
    "name": "自定义阶段（更新）",
    "description": "项目特定阶段，包含特殊要求",
    "defaultWeight": 15,
    "updatedAt": "2026-03-26T10:05:00Z"
  },
  "timestamp": "2026-03-26T10:05:00Z"
}
```

**错误响应**:
- 4004: 阶段不存在
- 2003: 系统预置阶段不能修改关键字段

---

### 4. 删除阶段

**接口**: `DELETE /api/v1/stages/:id`

**说明**: 删除阶段（仅自定义阶段）

**成功响应** (200):

```json
{
  "code": 200,
  "message": "删除成功",
  "data": null,
  "timestamp": "2026-03-26T10:00:00Z"
}
```

**错误响应**:
- 4004: 阶段不存在
- 2003: 系统预置阶段不能删除
- 2005: 阶段已被项目使用，不能删除

---

### 5. 添加任务模板

**接口**: `POST /api/v1/stages/:id/tasks`

**说明**: 为阶段添加任务模板

**请求参数**:

```json
{
  "key": "device_installation",
  "name": "设备安装",
  "description": "按照方案进行设备安装",
  "defaultWeight": 30,
  "materialRequirements": [
    {
      "key": "after_photo",
      "name": "安装后照片",
      "description": "设备安装完成后的照片",
      "fileType": "image",
      "required": true,
      "minCount": 5,
      "maxCount": 10,
      "acceptTypes": ["image/jpeg", "image/png"]
    }
  ]
}
```

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| key | string | 是 | 任务唯一标识 |
| name | string | 是 | 任务名称 |
| description | string | 否 | 任务描述 |
| defaultWeight | int | 否 | 默认权重 |
| materialRequirements | array | 否 | 资料需求数组 |

**成功响应** (200):

```json
{
  "code": 200,
  "message": "添加成功",
  "data": {
    "id": "task_002",
    "key": "device_installation",
    "name": "设备安装",
    "description": "按照方案进行设备安装",
    "defaultWeight": 30
  },
  "timestamp": "2026-03-26T10:00:00Z"
}
```

---

### 6. 更新任务模板

**接口**: `PUT /api/v1/stages/:stageId/tasks/:taskId`

**说明**: 更新阶段的任务模板

**请求参数**:

```json
{
  "name": "设备安装（更新）",
  "description": "按照方案进行设备安装和调试",
  "defaultWeight": 35,
  "materialRequirements": [
    {
      "key": "after_photo",
      "name": "安装后照片",
      "description": "设备安装完成后的照片",
      "fileType": "image",
      "required": true,
      "minCount": 5,
      "maxCount": 10,
      "acceptTypes": ["image/jpeg", "image/png"]
    },
    {
      "key": "install_report",
      "name": "安装报告",
      "description": "设备安装完成报告",
      "fileType": "document",
      "required": true,
      "minCount": 1,
      "maxCount": 1,
      "acceptTypes": ["application/pdf"]
    }
  ]
}
```

**成功响应** (200):

```json
{
  "code": 200,
  "message": "更新成功",
  "data": {
    "id": "task_002",
    "stageId": "stage_002",
    "name": "设备安装（更新）",
    "description": "按照方案进行设备安装和调试",
    "defaultWeight": 35
  },
  "timestamp": "2026-03-26T10:00:00Z"
}
```

---

### 7. 删除任务模板

**接口**: `DELETE /api/v1/stages/:stageId/tasks/:taskId`

**说明**: 删除阶段的任务模板

**成功响应** (200):

```json
{
  "code": 200,
  "message": "删除成功",
  "data": null,
  "timestamp": "2026-03-26T10:00:00Z"
}
```

---

## 错误码说明

### 错误码规范

| 错误码范围 | 说明 |
|-----------|------|
| 200 | 成功 |
| 1001-1999 | 认证授权错误 |
| 2001-2999 | 业务逻辑错误 |
| 3001-3999 | 参数验证错误 |
| 4001-4999 | 资源不存在错误 |
| 5001-5999 | 服务器内部错误 |

### 常用错误码

| 错误码 | 说明 | HTTP状态码 |
|--------|------|-----------|
| 200 | 操作成功 | 200 |
| 1001 | 用户名或密码错误 | 401 |
| 1002 | Token无效或已过期 | 401 |
| 1003 | Refresh Token无效 | 401 |
| 1004 | 用户已被禁用 | 403 |
| 1005 | 无权限访问 | 403 |
| 2001 | 资源已存在 | 400 |
| 2002 | 资源被引用，不能删除 | 400 |
| 2003 | 系统预置资源不能修改/删除 | 403 |
| 2004 | 资源下有关联数据，不能删除 | 400 |
| 2005 | 资源已被使用，不能删除 | 400 |
| 3001 | 必填参数缺失 | 400 |
| 3002 | 参数格式错误 | 400 |
| 3003 | 参数值超出范围 | 400 |
| 4004 | 资源不存在 | 404 |
| 5001 | 服务器内部错误 | 500 |
| 5002 | 数据库错误 | 500 |
| 5003 | 第三方服务错误 | 500 |

### 错误响应示例

```json
{
  "code": 1001,
  "message": "用户名或密码错误",
  "details": {
    "field": "username",
    "reason": "用户不存在"
  },
  "timestamp": "2026-03-26T10:00:00Z"
}
```

---

## 附录

### A. 权限编码规范

**格式**: `模块:资源:操作`

**示例**:
- `project:view` - 查看项目
- `project:create` - 创建项目
- `project:edit` - 编辑项目
- `project:delete` - 删除项目
- `device:view` - 查看设备
- `device:create` - 创建设备
- `*:*:*` 或 `*` - 所有权限（超级管理员）

### B. Token示例

**Access Token**:
```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VySWQiOiJ1c2VyXzAwMSIsInVzZXJuYW1lIjoiYWRtaW4iLCJyb2xlcyI6WyJhZG1pbiJdLCJleHAiOjE3MTQxNjAwMDAsImlhdCI6MTcxNDE1MjgwMH0.xxx
```

**Token Payload** (解码后):
```json
{
  "userId": "user_001",
  "username": "admin",
  "roles": ["admin"],
  "exp": 1714160000,
  "iat": 1714152800
}
```

### C. 接口调用示例

**使用cURL**:
```bash
# 登录
curl -X POST http://localhost:8080/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'

# 获取角色列表
curl -X GET http://localhost:8080/api/v1/roles \
  -H "Authorization: Bearer {token}"

# 创建角色
curl -X POST http://localhost:8080/api/v1/roles \
  -H "Authorization: Bearer {token}" \
  -H "Content-Type: application/json" \
  -d '{"code":"engineer","name":"实施工程师"}'
```

**使用Axios**:
```javascript
// 登录
const login = async (username, password) => {
  const response = await axios.post('/api/v1/auth/login', {
    username,
    password
  });
  return response.data;
};

// 获取角色列表
const getRoles = async (params) => {
  const response = await axios.get('/api/v1/roles', { params });
  return response.data;
};
```

---

**文档结束**
