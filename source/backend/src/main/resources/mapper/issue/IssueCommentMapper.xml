<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dataacquisition.modules.issue.mapper.IssueCommentMapper">

    <select id="selectByIssueId" resultType="com.dataacquisition.modules.issue.entity.IssueComment">
        SELECT
            c.id,
            c.issue_id,
            c.content,
            c.author_id,
            c.is_internal,
            c.create_time,
            u.name AS author_name
        FROM t_issue_comment c
        LEFT JOIN t_user u ON c.author_id = u.id AND u.deleted = 0
        WHERE c.issue_id = #{issueId}
        ORDER BY c.create_time ASC
    </select>

</mapper>
