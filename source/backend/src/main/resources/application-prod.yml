# 生产环境配置
server:
  port: 8080

spring:
  main:
    allow-circular-references: true
  datasource:
    url: jdbc:mysql://${DB_HOST:10.1.2.230}:3306/data_acquisition?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=Asia/Shanghai
    username: ${DB_USERNAME:data_acquisition}
    password: ${DB_PASSWORD:G1e86czRd5ttZVlV}

  data:
    redis:
      host: ${REDIS_HOST:10.1.2.230}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:6WJHKT8ahxYDOFwF}

minio:
  endpoint: ${MINIO_ENDPOINT:http://10.1.2.230:9000}
  accessKey: ${MINIO_ACCESS_KEY:65RD1skNPDqG}
  secretKey: ${MINIO_SECRET_KEY:9VQaHWcHU27t2tYC}
  bucketName: ${MINIO_BUCKET_NAME:data-acquisition}
  public-endpoint: ${MINIO_PUBLIC_ENDPOINT:https://pm.anosi.cn/minio}

dingtalk:
  app-key: ${DINGTALK_APP_KEY:}
  app-secret: ${DINGTALK_APP_SECRET:}
  agent-id: ${DINGTALK_AGENT_ID:}
  corp-id: ${DINGTALK_CORP_ID:}
  sync:
    enabled: ${DINGTALK_SYNC_ENABLED:true}
    cron: "0 0 2 * * ?"

logging:
  level:
    com.dataacquisition: info
    org.springframework.security: warn
  file:
    name: /var/log/data-acquisition/application.log

knife4j:
  production: true

# Actuator 健康检查配置
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
  health:
    defaults:
      enabled: true
