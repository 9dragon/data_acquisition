# 数据库迁移指导文档

## 手动执行数据库迁移

由于MySQL命令行工具在当前环境中不可用，请使用以下方式之一执行迁移：

### 方式1：使用Navicat/phpMyAdmin等图形化工具

1. 打开Navicat或phpMyAdmin
2. 连接到MySQL服务器：
   - 主机：localhost
   - 端口：3306
   - 用户名：root
   - 密码：application.yml中配置的密码（root或admin123）
3. 选择数据库 `data_acquisition`
4. 执行以下SQL脚本（按顺序）：
   - `source/deployment/config/mysql/migrations/02_add_permission_tables.sql`
   - `source/deployment/config/mysql/migrations/03_add_stage_tables.sql`

### 方式2：使用MySQL Workbench

1. 打开MySQL Workbench
2. 连接到本地MySQL实例
3. 选择 `data_acquisition` 数据库
4. 打开SQL脚本文件并执行

### 方式3：等后端启动后自动执行

后端启动时会自动执行 `init.sql`，但新增的迁移脚本需要手动执行。

---

## 验证迁移结果

执行以下SQL验证：

```sql
-- 检查权限表
SELECT COUNT(*) as permission_count FROM t_permission WHERE deleted = 0;

-- 检查阶段表
SELECT COUNT(*) as stage_count FROM t_stage WHERE deleted = 0;

-- 检查超级管理员权限
SELECT name, code, permissions FROM t_role WHERE code = 'ADMIN';

-- 应该看到：
-- permission_count = 20
-- stage_count = 6
-- permissions字段包含所有权限ID（逗号分隔）
```

---

## 如果数据库连接失败

检查 `application.yml` 中的数据库配置：
```yaml
datasource:
  url: jdbc:mysql://localhost:3306/data_acquisition?...
  username: root
  password: root  # 确认密码正确
```

如果密码不是 `root`，请修改为正确的密码（如 `admin123`）。
